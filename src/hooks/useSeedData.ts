'use client';

// No longer auto-seeds habits.
// New users start with a clean slate and add their own habits.
export function useSeedData() {
  // intentionally empty - kept to avoid breaking AppShell import
}
